---
interface Props {
  name: string;
  icon: string;
}

const { name, icon } = Astro.props;
---

<li class="skill">
  <div class="skill-icon"><img class="skill-image" src={icon} /></div>
  <h4 class="name">{name}</h4>
</li>

<style>
  .skill {
    display: flex;
    flex-direction: column;
    width: 7rem;
    align-items: center;
    justify-content: center;
    opacity: 0.8;
    transition: opacity 0.15s ease-in-out;
  }

  .skill:hover {
    opacity: 1;
  }

  .name {
    margin-top: 8px;
    font-weight: 300;
    font-size: 1rem;
  }

  .skill-icon {
    aspect-ratio: 1;
    height: auto;
    width: 60px;
    border-radius: 50%;
    padding: 10px;
    background-color: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  .skill-image {
    width: 48px;
    height: auto;
  }
</style>
